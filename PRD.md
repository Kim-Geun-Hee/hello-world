# PRD (Product Requirements Document)

## 1. 문서 정보
- 제품명: Hello World Static Web App
- 문서 버전: v1.0
- 작성일: 2026-08-19
- 대상 사용자: 개발 검증/배포 테스트를 수행하는 내부 사용자

## 2. 제품 개요
본 제품은 브라우저에서 `Hello World` 문자열을 빠르게 확인할 수 있는 최소 기능의 정적 웹 애플리케이션이다.
핵심 목적은 다음과 같다.
- Azure 정적 호스팅 배포 경로 검증
- 정적 콘텐츠 업로드/배포 프로세스 검증
- URL 접근성 및 기본 가용성 확인

## 3. 문제 정의
다음 요구가 반복적으로 발생한다.
- 클라우드 배포 파이프라인이 정상 동작하는지 빠르게 확인해야 함
- 백엔드 없이도 URL로 즉시 접근 가능한 샘플 앱이 필요함

## 4. 목표 및 비목표
### 4.1 목표 (Goals)
- 사용자가 URL 접속 시 첫 화면에서 `Hello World`를 확인한다.
- 페이지가 데스크톱/모바일에서 정상 렌더링된다.
- 배포 완료 후 공개 URL로 접근 가능하다.

### 4.2 비목표 (Non-Goals)
- 로그인/회원가입
- 데이터 저장/조회
- 동적 API 연동
- 다국어 전환

## 5. 사용자 시나리오
- 배포 담당자는 앱을 Azure에 배포한 뒤 URL을 연다.
- 사용자는 첫 화면 중앙의 `Hello World` 텍스트를 확인한다.
- 담당자는 결과를 통해 배포 성공 여부를 판단한다.

## 6. 기능 요구사항
### FR-1. 기본 페이지 노출
- 시스템은 루트 URL(`/`) 접속 시 `Hello World`를 표시해야 한다.

### FR-2. 반응형 뷰포트
- 시스템은 모바일/데스크톱에서 콘텐츠가 화면 중앙에 노출되도록 해야 한다.

### FR-3. 정적 호스팅
- 시스템은 정적 파일(`index.html`)만으로 서비스되어야 한다.

### FR-4. 공개 URL 접근
- 시스템은 인터넷 환경에서 공개 URL을 통해 접근 가능해야 한다.

## 7. 비기능 요구사항
### NFR-1. 성능
- 최초 페이지 로드 시간은 일반 네트워크 환경에서 체감상 즉시(약 2초 이내)를 목표로 한다.

### NFR-2. 가용성
- Azure 스토리지 정적 웹사이트 기본 SLA/가용성 범위를 따른다.

### NFR-3. 유지보수성
- 단일 HTML 파일 수정만으로 메시지/스타일 변경이 가능해야 한다.

### NFR-4. 보안
- 서버 사이드 코드 및 비밀키를 포함하지 않는다.

## 8. 성공 지표 (Success Metrics)
- URL 접속 시 `Hello World` 표시 성공률: 100%
- 배포 후 접근 가능 확인 시간: 5분 이내
- 배포 검증 실패율: 5% 미만

## 9. 수용 기준 (Acceptance Criteria)
- AC-1: 루트 URL 접속 시 H1 또는 본문에 `Hello World`가 포함되어야 한다.
- AC-2: 모바일(폭 390px 기준)에서 텍스트가 잘리지 않고 중앙 배치되어야 한다.
- AC-3: 데스크톱(폭 1280px 이상)에서 레이아웃 깨짐이 없어야 한다.
- AC-4: 외부 네트워크에서 URL 접근 시 HTTP 200 응답을 받아야 한다.

## 10. 릴리스 범위
### In Scope
- `index.html` 제공
- Azure 정적 웹사이트 배포
- URL 검증

### Out of Scope
- 커스텀 도메인
- CI/CD 자동화
- 로깅/모니터링 대시보드

## 11. 리스크 및 대응
- 리스크: 스토리지 계정/리소스 설정 실수로 URL 비노출
- 대응: 배포 후 URL과 콘텐츠 자동 검증 커맨드 사용

## 12. 향후 확장
- 커스텀 도메인 연결
- GitHub Actions 기반 자동 배포
- 간단한 상태 체크 모니터링 추가
